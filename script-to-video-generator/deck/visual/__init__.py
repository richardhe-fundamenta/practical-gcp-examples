"""The renderer: LLM-authored, visualization-heavy Rough.js scenes.

The model AUTHORs bespoke drawing code per paragraph, statically safety-vetted,
run against a fixed sync harness so the recorder/voice-sync is shared unchanged.
"""
